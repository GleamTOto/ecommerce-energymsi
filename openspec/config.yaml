# openspec/config.yaml — ecommerce-energymsi
# Auto-generated by sdd-init on 2026-08-05

schema: spec-driven

context: |
  Tech stack: Next.js 16 (App Router), TypeScript 5, React 19, Tailwind CSS v4, shadcn/ui (new-york)
  State management: Zustand 5 | Forms: react-hook-form + zod | Auth: next-auth v5 beta
  ORM: Prisma 7 (PostgreSQL 16) | Payments: Stripe | Images: Cloudinary
  Architecture: Route groups (shop, admin-panel, auth), API route handlers, server components default
  Style: Tailwind CSS v4 + CSS variables (OKLCH), dark/light themes via next-themes
  Path alias: @/* -> ./src/*

rules:
  proposal:
    - Include rollback plan for risky changes
    - Consider impact on both shop and admin-panel route groups
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
    - Prefer dedicated pages over modals for forms (project rule)
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns and component organization
    - Server Components by default, "use client" only for interactivity
    - Use Route Handlers for API endpoints (not Server Actions)
    - Use Zustand for global state, react-hook-form + zod for forms
    - Use cn() utility from @/lib/utils for class merging
    tdd: false
    test_command: "npx eslint"
  verify:
    test_command: "npx eslint"
    build_command: "npm run build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
